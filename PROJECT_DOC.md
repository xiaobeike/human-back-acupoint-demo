# 项目文档

## 1. 项目目标

做一个可在 Android 手机上运行的`背部穴位可视化展示原型`，用于理疗机器人、康复设备、穴位数字化方案的前期展示与交互验证。

这个项目的当前目标不是自动识别所有人体解剖结构，也不是控制真实机器人执行治疗，而是先把下面这条能力链路打通：

- 背部画面采集
- 人体轮廓模板定位
- 关键锚点手动校准
- 穴位规则生成
- JSON 数据输出
- 模拟设备接收展示

## 2. 当前版本定位

当前版本是：

- Android 原生 Demo
- 演示型原型
- 手动定位 + 规则映射方案

当前版本不是：

- 医疗级自动定穴系统
- 临床产品
- 真实机器人执行系统

## 3. 为什么第一版不用自动识别

对背部来说，第一版如果直接追求自动识别，很容易出现几个问题：

- 背部表面特征弱
- 肩胛、脊柱中线、腰侧轮廓易受姿态影响
- 衣物遮挡明显
- 呼吸会带来动态形变
- 2D 误差会被机器人接触动作放大

所以当前版本先选择更稳定的策略：

- 固定轮廓模板
- 可解释的人工校准
- 锁定后批量生成穴位

这样更适合能力展示，也更适合在没有真实设备协议的阶段快速迭代。

## 4. 当前已完成能力

### 4.1 视觉与交互

- CameraX 后摄实时预览
- 背部固定轮廓模板
- 三种体型模板：
  - 偏瘦
  - 标准
  - 偏宽
- 三步式定位流程：
  - 肩线
  - 脊柱中线
  - 腰侧胖瘦
- 六个锚点拖拽校准
- 锁定后显示穴位

### 4.2 穴位与数据

- 30 个背部演示穴位
- 整套穴位 JSON
- 当前选中穴位的单点 JSON
- 整体页面查看 JSON
- 单点页面查看 JSON

### 4.3 展示链路

- 主演示页
- JSON 数据详情页
- 当前穴位 JSON 页
- 模拟设备接收页
- 当前画面导出

## 5. 当前交互流程

1. 打开 App
2. 后摄预览人体背部
3. 选择体型模板
4. 点击开始校准
5. 依次完成：
   - 对齐肩线
   - 对齐脊柱中线
   - 调整腰侧胖瘦
6. 锁定定位
7. 系统显示穴位
8. 用户点选某个穴位
9. 用户可以：
   - 查看整套 JSON
   - 查看当前点 JSON
   - 发送到模拟设备
10. 模拟设备接收页显示当前点位已被接收

## 6. 当前定位模型

当前不是 AI 自动检测，而是`规则驱动 Demo`。

### 6.1 锚点

当前有 6 个锚点：

- 左肩点
- 右肩点
- 脊柱上端
- 脊柱下端
- 左腰侧轮廓
- 右腰侧轮廓

### 6.2 计算逻辑

- 脊柱上端到下端构成纵向主轴
- 左右肩点定义上部横向尺度
- 左右腰点定义下部横向尺度
- 不同高度处的横向宽度由肩宽与腰宽插值混合
- 每个穴位按固定的：
  - `vertical_t`
  - `lateral_t`
  - 左/右/中线侧别
 生成像素位置

这意味着：

- 先把身体轮廓定位好
- 其余穴位就可以按同一规则批量确定

## 7. 当前穴位范围

总计 `30` 个背部演示穴位。

### 7.1 督脉

- 陶道
- 大椎
- 神道
- 命门

### 7.2 膀胱经双侧俞穴

- 大杼
- 风门
- 肺俞
- 厥阴俞
- 心俞
- 膈俞
- 肝俞
- 胆俞
- 脾俞
- 胃俞
- 三焦俞
- 肾俞
- 大肠俞

## 8. JSON 结构

### 8.1 整套穴位 JSON

包含：

- 版本号
- 模板名
- 采集时间
- 画布尺寸
- 身体框架参数
  - 肩宽
  - 腰宽
  - 脊柱轴长度
- 6 个锚点坐标
- 全部穴位列表

### 8.2 单点穴位 JSON

包含：

- 整套结构中的公共信息
- 当前选中穴位
- `dispatch_mode = single_acupoint`

### 8.3 当前坐标类型

当前 JSON 里有三类坐标：

- 像素坐标
- 归一化坐标
- 身体相对坐标

其中身体相对坐标是当前版本最适合演示“设备可消费能力”的部分。

## 9. 当前页面结构

### 9.1 MainActivity

职责：

- 相机权限
- CameraX 预览
- 三步校准状态机
- 锁定后穴位交互
- 整套 JSON / 单点 JSON / 模拟设备入口

### 9.2 BackOverlayView

职责：

- 背部轮廓绘制
- 参考线绘制
- 锚点拖拽
- 穴位绘制
- 穴位点击选中

### 9.3 PayloadViewerActivity

职责：

- 展示整套 JSON
- 展示当前点 JSON
- 复制 JSON

### 9.4 DeviceReceiverActivity

职责：

- 展示模拟设备已接收
- 显示当前穴位信息
- 显示收到的单点 JSON

## 10. 为什么要做模拟设备接收页

因为当前没有真实设备协议。

如果只做“复制 JSON”或“系统分享”，对展示来说不够直观。模拟设备接收页的价值在于：

- 把“发送”变成明确的链路终点
- 让演示对象看到设备侧结果
- 更容易说明未来真实设备接入时会发生什么

所以当前版本虽然没有真实设备通信，但已经能演示：

- 数据如何生成
- 数据如何被选中
- 数据如何被设备侧消费

## 11. 当前边界

当前版本不包含：

- 自动背部人体分割
- 自动 C7 / 肩峰 / 肩胛识别
- 深度相机输入
- 机器人坐标系转换
- 力控与安全禁区
- 真实设备通信协议

## 12. 适合开源吗

适合。

原因：

- 目标清晰
- 结构独立
- 无私有后端依赖
- 无商业协议耦合
- 对 Android / 机器人 / 康复设备方向都有参考价值

开源时建议明确写在 README：

- 这是展示原型
- 不是医疗产品
- 当前靠规则映射，不是自动 AI 定穴
- 欢迎基于该原型继续接 AI 识别和设备协议

## 13. 建议的 GitHub 仓库信息

### 仓库名建议

- `human-back-acupoint-demo`
- `android-back-acupoint-demo`
- `rehab-robot-acupoint-demo`

### 仓库描述建议

`Android native demo for manual back contour alignment, acupoint overlay, JSON export, and simulated device receiving workflow.`

## 14. 后续路线

### 阶段 2

- 自动背部锚点初值
- 人工微调保留
- 更稳定的定位流程

### 阶段 3

- 多穴位治疗序列
- 真实设备通信
- 设备端状态回传

### 阶段 4

- 深度/三维定位
- 相机到机器人外参标定
- 力控和安全策略

## 15. 结论

当前这个 Demo 已经能完整演示一条很清晰的能力链路：

- 人背定位
- 穴位生成
- 数据输出
- 模拟设备接收

对开源展示、方案说明、前期能力验证来说，已经有足够完整性。
